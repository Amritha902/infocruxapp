// This file contains your Firebase project's configuration.
// IMPORTANT: You need to replace the placeholder values below with the actual
// configuration values from your Firebase project.
//
// You can find your Firebase project configuration in the Firebase console:
// 1. Go to the Firebase console (https://console.firebase.google.com/).
// 2. Select your project.
// 3. In the project overview, click the "</>" (Web) icon to find your config.
//
// If you haven't created a Firebase project yet, you'll need to do that first.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
  measurementId: "YOUR_MEASUREMENT_ID" // Optional: for Google Analytics
};
